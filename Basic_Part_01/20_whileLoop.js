//while loop

// 0  se 9

let initializeValue = 0;
while (initializeValue < 9) {
  console.log(initializeValue);
  initializeValue++;
}

let i = 0;
for (i; i < 9; i++) {
  const element = i;
  console.log(element);
}
